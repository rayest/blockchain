### 使用  webpack 管理项目
```shell
$ mkdir webpack-demo
$ cd webpack-demo

$ npm init -y
$ npm install webpack webpack-cli --save-dev
$ npm install alchemy-sdk


$ touch webpack.config.js
$ touch src/index.js

# package 
$ npx webpack

```
